
substitutions:
  devicename: mailbox
  upper_devicename: Mailbox

esphome:
  name: ${devicename}
  platform: ESP32
  board: nodemcu-32s


wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

# captive_portal:

mqtt:
  broker: !secret broker_ip
  discovery: true
  # keepalive: 600s
  # on_message:
    # topic: ${devicename}/ota_mode
    # payload: 'ON'
    # then:
    #   - deep_sleep.prevent: deep_sleep_1

logger:
  level: DEBUG

ota:

# deep_sleep:
#   id: deep_sleep_1
#   run_duration: 20s
#   sleep_duration: 1min

